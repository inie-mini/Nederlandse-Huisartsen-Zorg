import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'Nederlandse-Huisartsen-Zorg',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
